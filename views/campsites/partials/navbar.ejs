<nav id="nav-bar" class="navbar navbar-expand-lg border-bottom position-fixed navbar-light w-100">
    <div class="container">
        <a id="sm-display" class="navbar-brand fw-bold" href="/">Campspot</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse d-lg-flex justify-content-between align-items-center" id="navbarNavAltMarkup">
            <div id="lg-display" class="navbar-nav">
                <a id="nav-title" class="navbar-brand nav-link fw-bold" href="/">Campspot</a>
            </div>
            <div class="navbar-nav rounded-pill p-1 align-items-center">
                <a class="nav-link text-dark border-end py-0 px-3 home-anchor" href="/campsites">Home</a>
                <a class="nav-link border-1 border-end py-0 px-3 text-dark" aria-current="page" href="/campsites/map">Map</a>
                <% if (currentUser) { %>
                    <a class="nav-link text-dark px-3" href="/campsites/new">campsite</a>
                <% } else { %>
                    <a class="nav-link text-muted px-3" href="/campsites/new">campsite</a>
                <% } %>
            </div>
            
            <div class="navbar-nav">
                <% if (!currentUser) { %>             
                    <a class="nav-link" href="/users/login">Log in</a>
                    <a class="nav-link" href="/users/register">Sign up</a>
                    <% } else { %>
                    <div id="space"></div>
                    <a class="nav-link" href="/users/logout">Logout</a>
                <% } %>
            </div>
        </div>
</nav>
